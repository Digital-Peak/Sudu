# Requirements
The following list outlines the requirements for server and browser.

## Server
PHP Version: >= 8

## Browser
Safari: >= 9
Firefox: >= 32
Chrome: >= 45
Opera: >= 32
Edge: >= 12
Internet Explorer: No support
